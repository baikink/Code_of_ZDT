# 步进电机测试程序说明

## 已完成的移植工作

### 1. 文件复制
✅ `Emm_V5.c` → `Core/Src/Emm_V5.c`
✅ `Emm_V5.h` → `Core/Inc/Emm_V5.h`

### 2. 代码修改
✅ `main.c` 中添加 `#include "Emm_V5.h"`
✅ 添加初始化测试代码
✅ 添加循环往返运动测试

## 测试程序说明

### 启动时执行（只执行一次）

```c
// 正向移动到位置 10000
Emm_V5_Pos_Control(1, 0, 1000, 0, 10000, 1, 0);
等待 3 秒

// 反向回到位置 0
Emm_V5_Pos_Control(1, 1, 1000, 0, 0, 1, 0);
等待 3 秒
```

**目的**：验证电机能否响应命令

### 主循环（持续执行）

```c
while(1) {
    // 移动到位置 5000
    Emm_V5_Pos_Control(1, 0, 800, 0, 5000, 1, 0);
    等待 2 秒
    
    // 回到位置 0
    Emm_V5_Pos_Control(1, 1, 800, 0, 0, 1, 0);
    等待 2 秒
}
```

**目的**：观察电机往返运动，测试稳定性

## 参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| addr | 1 | 电机地址（出厂默认1） |
| dir | 0/1 | 0=正向，1=反向 |
| vel | 800-1000 | 速度（RPM），建议先用中低速 |
| acc | 0 | 加速度（0=直接启动） |
| clk | 0-10000 | 目标脉冲数（位置） |
| raF | 1 | 绝对位置模式 |
| snF | 0 | 不启用多机同步 |

## 测试步骤

### 1. 硬件连接检查

**STM32F407VGT6 ↔ 步进电机驱动器**
- TX1 (PA9) → 驱动器 RX
- GND → 驱动器 GND
- 驱动器供电：24V 电源

**驱动器 ↔ 步进电机**
- A+, A-, B+, B- 接电机四线

### 2. 编译下载

1. 用 Keil MDK 或 STM32CubeIDE 打开工程
2. 编译（Build）
3. 下载到 STM32F407VGT6
4. 复位运行

### 3. 观察现象

**正常现象**：
- 上电后：电机先转到 10000 位置，停 3 秒，再回到 0
- 进入循环：电机在 0 和 5000 之间往返运动
- 运动平滑，无异响

**异常处理**：

| 现象 | 可能原因 | 解决方法 |
|------|---------|---------|
| 电机不动 | 串口未连接/波特率不对 | 检查 UART1 配置（115200, 8N1） |
| 电机抖动 | 供电不足 | 检查 24V 电源 |
| 方向反了 | dir 参数设置 | 交换 dir 的 0 和 1 |
| 转速太快/慢 | vel 参数 | 调整 vel（500-2000） |

## 下一步调试建议

### 测试 1：方向确认
修改代码，测试 dir=0 和 dir=1 分别往哪边转：
```c
Emm_V5_Pos_Control(1, 0, 500, 0, 3000, 1, 0);  // dir=0
HAL_Delay(3000);
Emm_V5_Pos_Control(1, 1, 500, 0, 0, 1, 0);     // dir=1
```

### 测试 2：速度对比
尝试不同速度：
```c
Emm_V5_Pos_Control(1, 0, 300, 0, 5000, 1, 0);   // 慢速
Emm_V5_Pos_Control(1, 0, 1000, 0, 5000, 1, 0);  // 中速
Emm_V5_Pos_Control(1, 0, 2000, 0, 5000, 1, 0);  // 快速
```

### 测试 3：机械标定（重要！）

**目的**：确定脉冲数和角度的关系

1. 让滑槽处于水平状态，记为 0° 位置
2. 读取当前电机位置（假设为 P0）
3. 发送命令移动 1000 脉冲：
   ```c
   Emm_V5_Pos_Control(1, 0, 500, 0, P0+1000, 1, 0);
   ```
4. 用量角器测量滑槽实际倾角 θ
5. 计算：**K = 1000 / θ** （脉冲/度）
6. 重复测试多个角度，取平均值

### 测试 4：安全行程确认

找出滑槽的最大安全倾角：
- 正向最大角度 → 对应脉冲数 P_max
- 反向最大角度 → 对应脉冲数 P_min
- 在代码中设置限位保护

## 注意事项

⚠️ **初次测试时**：
1. 速度用 500-800 RPM（较慢）
2. 移动距离不要太大（3000-5000 脉冲）
3. 手动能随时断电

⚠️ **机械安全**：
1. 确认丝杆和滑块连接牢固
2. 检查是否有机械卡死的可能
3. 设置软件限位保护

⚠️ **电气安全**：
1. 24V 电源容量足够（建议 ≥ 3A）
2. 接地良好
3. 避免短路

## 后续工作

当电机调试完成后，需要：
1. ✅ 确认脉冲-角度关系（K 值）
2. ✅ 确认安全行程范围
3. ⏳ 设计串口通信协议（MaixCAM → STM32）
4. ⏳ 实现 PID 控制算法
5. ⏳ 整合钢球检测和电机控制

## 常用调试命令

```c
// 移动到绝对位置
Emm_V5_Pos_Control(addr, dir, vel, acc, position, 1, 0);

// 相对移动（相对当前位置）
Emm_V5_Pos_Control(addr, dir, vel, acc, steps, 2, 0);

// 急停
Emm_V5_Stop_Now(addr, 1, 0);

// 读取当前位置
Emm_V5_Read_Sys_Params(addr, S_CPOS);
```

## 问题排查

遇到问题时，请提供：
1. 电机的具体表现（不动/抖动/方向错误）
2. 串口配置（波特率、数据位、停止位）
3. 供电电压
4. 是否有报错信息
