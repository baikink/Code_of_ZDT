# 代码移植完成检查清单

## ✅ 已完成的修改

### 1. 驱动文件
- ✅ 复制 `Emm_V5.c` → `Core/Src/`
- ✅ 复制 `Emm_V5.h` → `Core/Inc/`

### 2. usart.c 修改
- ✅ 添加 `#include "Emm_V5.h"`
- ✅ 添加全局变量：
  ```c
  __IO bool rxFrameFlag = false;
  __IO uint8_t rxCmd[CMD_LEN] = {0};
  __IO uint8_t rxCount = 0;
  ```
- ✅ GPIO 配置改为 `GPIO_PULLUP`（上拉）
- ✅ DMA RX 优先级改为 `DMA_PRIORITY_HIGH`
- ✅ DMA TX 优先级改为 `DMA_PRIORITY_HIGH`

### 3. stm32f4xx_it.c 修改
- ✅ 添加 `#include "Emm_V5.h"`
- ✅ 添加 UART 空闲中断处理代码：
  ```c
  if(__HAL_UART_GET_FLAG(&huart1, UART_FLAG_IDLE) != RESET)
  {
    __HAL_UART_CLEAR_IDLEFLAG(&huart1);
    HAL_UART_DMAStop(&huart1);
    rxCount = CMD_LEN - __HAL_DMA_GET_COUNTER(&hdma_usart1_rx);
    rxFrameFlag = true;
    HAL_UART_Receive_DMA(&huart1, (uint8_t*)rxCmd, CMD_LEN);
  }
  ```

### 4. main.c 修改
- ✅ 添加 `#include "Emm_V5.h"`
- ✅ 启动 DMA 接收和空闲中断
- ✅ 添加启动测试代码
- ✅ 添加循环往返测试代码

## 与例程的对比

| 项目 | 例程 | 新工程 | 状态 |
|------|------|--------|------|
| UART 波特率 | 115200 | 115200 | ✅ 一致 |
| GPIO 上拉 | PULLUP | PULLUP | ✅ 已修改 |
| DMA RX 优先级 | HIGH | HIGH | ✅ 已修改 |
| DMA TX 优先级 | HIGH | HIGH | ✅ 已修改 |
| 空闲中断处理 | 有 | 有 | ✅ 已添加 |
| 全局变量 | 有 | 有 | ✅ 已添加 |
| DMA 接收启动 | 有 | 有 | ✅ 已添加 |

## 关键差异说明

### 时钟配置不同
**例程**：使用外部晶振（HSE）+ PLL，系统时钟 168MHz
**新工程**：使用内部时钟（HSI），系统时钟 16MHz

**影响**：
- 串口时钟源不同，但波特率误差应该在可接受范围内
- 如果通信不稳定，可能需要在 CubeMX 中修改为 HSE + PLL

### 测试程序略有不同
**例程**：只在启动时执行一次，需要等待接收标志
**新工程**：启动测试 + 循环往返测试

## 编译前检查

### 检查 Emm_V5.h 中的宏定义

打开 `Core/Inc/Emm_V5.h`，确认以下宏定义存在：
```c
#define CMD_LEN 16    // 或其他值
```

如果没有，需要添加到头文件中。

### 检查项目配置

1. **Keil MDK**：
   - 确认 `Core/Src/Emm_V5.c` 已加入工程
   - Include Path 包含 `Core/Inc`

2. **STM32CubeIDE**：
   - 刷新项目（F5）
   - 确认文件出现在 Project Explorer

## 可能遇到的编译错误

### 错误1：`CMD_LEN` 未定义
**原因**：`Emm_V5.h` 中缺少宏定义
**解决**：添加 `#define CMD_LEN 16` 到头文件

### 错误2：`rxCmd` 重复定义
**原因**：多个文件定义了相同变量
**解决**：确保只在 `usart.c` 中定义，头文件中使用 `extern`

### 错误3：找不到 `Emm_V5.h`
**原因**：Include Path 未配置
**解决**：添加 `Core/Inc` 到 Include 路径

## 下载运行前检查

### 硬件连接
- [ ] STM32 TX1 (PA9) → 驱动器 RX
- [ ] STM32 GND → 驱动器 GND
- [ ] 驱动器 24V 供电正常
- [ ] 电机连接到驱动器

### 驱动器设置
- [ ] 驱动器地址设为 1（出厂默认）
- [ ] 驱动器波特率 115200（出厂默认）
- [ ] 驱动器已上电

## 预期运行效果

### 上电后（前 7 秒）
1. 延时 500ms
2. 电机正向转动到位置 10000
3. 延时 3000ms
4. 电机反向转动回位置 0
5. 延时 3000ms

### 进入主循环（持续）
1. 电机转到位置 5000
2. 延时 2000ms
3. 电机回到位置 0
4. 延时 2000ms
5. 重复

## 调试建议

### 如果电机不动
1. 检查串口波特率是否正确（115200）
2. 用示波器或逻辑分析仪查看 PA9 是否有输出
3. 检查驱动器指示灯状态
4. 尝试降低时钟到例程的配置（HSE + PLL）

### 如果电机运动异常
1. 检查方向是否正确（可能需要交换 dir 值）
2. 降低速度参数（500 RPM）
3. 减小移动距离（3000 脉冲）

### 如果编译错误
1. 查看"可能遇到的编译错误"部分
2. 确认所有文件都已正确添加
3. 清理项目后重新编译

## 时钟配置建议（可选）

如果通信不稳定，建议修改为与例程一致的时钟配置：

**在 CubeMX 中**：
1. Clock Configuration 页面
2. HSE 选择 8MHz 外部晶振
3. PLL 配置：
   - PLLM = 8
   - PLLN = 336
   - PLLP = 2
   - 系统时钟 = 168MHz
4. 重新生成代码

## 后续工作

移植完成并测试通过后：
1. ✅ 确认电机能正常运动
2. ⏳ 进行机械标定（脉冲-角度关系）
3. ⏳ 测试不同速度和位置参数
4. ⏳ 开发 MaixCAM → STM32 串口通信
5. ⏳ 实现 PID 控制算法
